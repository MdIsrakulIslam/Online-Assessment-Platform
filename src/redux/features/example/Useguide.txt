1. Get All Example Data

  const { data, isLoading, isError } = useGetAllExampleQuery({
    queryObj: [
      { name: "page", value: "1" },
      { name: "limit", value: "10" },
    ],
  });

2. Get a Single Example

  const { data, isLoading } = useGetSingleExampleQuery(id);

3. Create Example

 const [createExample, { isLoading }] = useCreateExampleMutation();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    try {
      const res = await createExample({ name }).unwrap();
      console.log("Created successfully:", res);
    } catch (err) {
      console.error("Error creating:", err);
    }
  };

4. Update Example

 const [updateExample, { isLoading }] = useUpdateExampleMutation();

  const handleUpdate = async () => {
    const formData = new FormData();
    formData.append("name", "Updated Name");

    try {
      const res = await updateExample({ id, formData }).unwrap();
      console.log("Updated:", res);
    } catch (err) {
      console.error("Update failed:", err);
    }
  };

  5. Delete Example

  const [deleteExample, { isLoading }] = useDeleteExampleMutation();

  const handleDelete = async () => {
    if (!confirm("Are you sure you want to delete this?")) return;
    try {
      await deleteExample(id).unwrap();
      console.log("Deleted successfully");
    } catch (err) {
      console.error("Delete failed:", err);
    }
  };
